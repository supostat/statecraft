<h1>Orders</h1>

<p class="filters">
  <%= link_to "all", orders_path %>
  <% OrderFlow.states.each do |state| %>
    <%= link_to state, orders_path(state: state) %>
  <% end %>
</p>

<table>
  <thead>
    <tr><th>number</th><th>kind</th><th>state</th></tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= link_to order.number, order_path(order) %></td>
        <td><%= order.is_a?(CreditOrder) ? "credit" : "regular" %></td>
        <td><code><%= order[:state] %></code></td>
      </tr>
    <% end %>
  </tbody>
</table>
